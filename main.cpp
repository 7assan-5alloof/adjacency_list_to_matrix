#include <bits/stdc++.h>
#include <vector>

using namespace std;

int main()
{
    freopen("a.in", "r", stdin);
    int n;
    cin >> n;
    vector<vector<int>> outgoing_edges;
    for (int i = 0; i < n; i < n; i++)
    {

    }

    return 0;
}
